# نظام تسجيل الدخول للمستخدمين الإداريين

## نظرة عامة
تم تركيب نظام تسجيل دخول كامل للمستخدمين الإداريين في لوحة تحكم مساحة العمل. النظام يتضمن مصادقة آمنة وإدارة للمستخدمين الإداريين.

## المميزات

### 🔐 نظام المصادقة
- **تسجيل الدخول**: صفحة دخول آمنة مع التحقق من البيانات
- **تذكر المستخدم**: خيار "تذكرني" للبقاء مسجل دخول
- **تسجيل الخروج**: تسجيل خروج آمن مع تنظيف الجلسة
- **حماية الصفحات**: جميع صفحات لوحة التحكم محمية

### 👤 إدارة الملف الشخصي
- **تحديث المعلومات**: تعديل الاسم والبريد الإلكتروني والهاتف
- **تغيير كلمة المرور**: تغيير آمن لكلمة المرور مع التحقق
- **معلومات إضافية**: عرض آخر تسجيل دخول وحالة الحساب

### 🛡️ الأمان
- **Middleware الحماية**: AdminMiddleware للتحقق من الصلاحيات
- **تشفير كلمات المرور**: تشفير آمن باستخدام Hash
- **تنظيف الجلسات**: تنظيف آمن عند تسجيل الخروج
- **تسجيل آخر دخول**: تتبع وقت آخر تسجيل دخول

## المستخدمين الافتراضيين

تم إنشاء مستخدمين إداريين افتراضيين:

### مدير النظام
- **البريد الإلكتروني**: `admin@branchhub.com`
- **كلمة المرور**: `admin123`
- **النوع**: `admin`
- **الصلاحيات**: كاملة

### مستخدم إداري
- **البريد الإلكتروني**: `manager@branchhub.com`
- **كلمة المرور**: `manager123`
- **النوع**: `manager`
- **الصلاحيات**: إدارية

## هيكل النظام

### الملفات المضافة/المعدلة

#### Controllers
- `app/Http/Controllers/AuthController.php` - إدارة المصادقة والملف الشخصي

#### Middleware
- `app/Http/Middleware/AdminMiddleware.php` - حماية الصفحات الإدارية

#### Views
- `resources/views/auth/login.blade.php` - صفحة تسجيل الدخول
- `resources/views/auth/profile.blade.php` - صفحة الملف الشخصي
- `resources/views/welcome.blade.php` - صفحة الترحيب

#### Routes
- `routes/web.php` - إضافة routes المصادقة والحماية

#### Database
- `database/migrations/2025_08_23_033622_add_last_login_at_to_users_table.php`
- `database/migrations/2025_08_23_033852_update_user_type_enum_in_users_table.php`
- `database/seeders/AdminUserSeeder.php`

#### Configuration
- `bootstrap/app.php` - تسجيل AdminMiddleware

## كيفية الاستخدام

### 1. الوصول للنظام
- افتح المتصفح واذهب إلى: `http://your-domain.com`
- ستظهر صفحة الترحيب
- اضغط على "تسجيل الدخول للوحة التحكم"

### 2. تسجيل الدخول
- استخدم بيانات أحد المستخدمين الافتراضيين
- أو أنشئ مستخدم جديد من نوع `admin` أو `manager`

### 3. إدارة الملف الشخصي
- اضغط على اسم المستخدم في الشريط الجانبي
- اختر "الملف الشخصي"
- يمكنك تحديث المعلومات أو تغيير كلمة المرور

### 4. تسجيل الخروج
- اضغط على اسم المستخدم في الشريط الجانبي
- اختر "تسجيل الخروج"

## الأمان

### حماية الصفحات
جميع صفحات لوحة التحكم محمية بـ:
- `auth` middleware - للتحقق من تسجيل الدخول
- `admin` middleware - للتحقق من نوع المستخدم

### أنواع المستخدمين
- `admin` - مدير النظام (صلاحيات كاملة)
- `manager` - مستخدم إداري (صلاحيات إدارية)
- `hourly`, `subscription` - مستخدمين عاديين

### تشفير البيانات
- كلمات المرور مشفرة باستخدام `Hash::make()`
- الجلسات محمية ومُنظفة عند تسجيل الخروج

## إضافة مستخدمين جدد

### من خلال Seeder
```bash
php artisan db:seed --class=AdminUserSeeder
```

### من خلال Tinker
```php
php artisan tinker

User::create([
    'name' => 'اسم المستخدم',
    'email' => 'user@example.com',
    'password' => Hash::make('password123'),
    'user_type' => 'admin', // أو 'manager'
    'phone' => '0500000000',
    'status' => 'active',
]);
```

## استكشاف الأخطاء

### مشاكل شائعة

1. **خطأ في تسجيل الدخول**
   - تأكد من صحة البريد الإلكتروني وكلمة المرور
   - تأكد من أن المستخدم من نوع `admin` أو `manager`

2. **خطأ في الوصول للصفحات**
   - تأكد من تسجيل الدخول
   - تأكد من نوع المستخدم

3. **مشاكل في قاعدة البيانات**
   - تأكد من تشغيل جميع migrations
   - تأكد من تشغيل seeders

### أوامر مفيدة
```bash
# تشغيل migrations
php artisan migrate

# تشغيل seeders
php artisan db:seed --class=AdminUserSeeder

# مسح cache
php artisan cache:clear
php artisan config:clear
php artisan route:clear

# إعادة تشغيل الخادم
php artisan serve
```

## التطوير المستقبلي

### ميزات مقترحة
- [ ] إدارة المستخدمين الإداريين من لوحة التحكم
- [ ] نظام أدوار وصلاحيات متقدم
- [ ] تسجيل نشاط المستخدمين
- [ ] إشعارات الأمان
- [ ] المصادقة الثنائية (2FA)
- [ ] استعادة كلمة المرور

### تحسينات الأمان
- [ ] تقييد محاولات تسجيل الدخول
- [ ] تسجيل محاولات الدخول الفاشلة
- [ ] تشفير إضافي للبيانات الحساسة
- [ ] فحص الأمان التلقائي

---

**ملاحظة**: تأكد من تغيير كلمات المرور الافتراضية في بيئة الإنتاج! 